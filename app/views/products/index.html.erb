<h1>Products#index</h1>
<p>Find me in app/views/products/index.html.erb</p>

<% @products.each do |product| %>

  商品<%=product.id%>(<%= product.name %>,<%=product.price%>
  <%= link_to '削除', product_path(id: product.id), method: :delete %>
  <%= link_to 'カート追加', "" %><br>
  

<% end %><br>
<%= link_to '商品を追加する', new_product_path %>
<%= link_to 'カートを見る' , cart_path(id: @cart.id) %>