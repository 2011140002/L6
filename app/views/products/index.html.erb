<% @products.each do |product| %>

  商品<%=product.id%>(<%= product.name %>,<%=product.price%>円)
  <%= link_to '削除', product_path(id: product.id), method: :delete %>
  <%= link_to 'カート追加', new_cart_item_path(product_id: product.id) %><br>
  

<% end %><br>
<%= link_to '商品を追加する', new_product_path %>
<%= link_to 'カートを見る' , "" %>